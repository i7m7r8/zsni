# wrangler.toml
name = "edgetunnel"
main = "_worker.js"
compatibility_date = "2024-12-28"

# --- Variables (can be set here or in Cloudflare Dashboard) ---
[vars]
ADMIN = "your-strong-admin-password" # Required: Admin panel password
UUID = "90cd4a77-141a-43c9-991b-08263cfe9c10" # Optional: Primary UUID
# PROXYIP = "proxyip.cmliussss.net:443" # Optional: Custom Proxy IP
# KEY = "CMLiussss"                    # Optional: Quick subscription key

# --- Production Environment (Example) ---
[env.production]
name = "edgetunnel-prod"
